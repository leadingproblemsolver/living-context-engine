# Required when binding the HTTP API to 0.0.0.0 or another non-loopback host.
LCE_API_TOKEN=replace-with-a-long-random-token
# Optional exact browser origin, for example https://context.example.com
LCE_CORS_ORIGIN=
