# Assumptions

Assumption: We assume the corpus fits comfortably on local disk for a single-user deployment.
Assumption: We assume operators will run sync before every consequential query or compile.
