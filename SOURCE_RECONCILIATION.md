# Source Reconciliation

The uploaded directory named `living-context-engine` contained a divergent DriftGuard web application while its repository metadata described a local SQLite/CLI context engine. The already-finalized DriftGuard repository was not changed. This release implements the distinct Living Context Engine described by the metadata and records the mismatch rather than publishing a second conflicting DriftGuard artifact.
